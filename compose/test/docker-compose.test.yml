version: '3'
services:
  unbfeelings-test:
    image: unbfeelings/unbfeelings:test
    build:
      context: ../../
      dockerfile: ./compose/test/Dockerfile
    environment:
      - MODE_ENVIROMENT=development
